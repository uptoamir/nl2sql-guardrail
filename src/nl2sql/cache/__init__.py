"""Caches — semantic / prompt / schema. v0 ships honest stubs.

Per `final_1.md` §8 + §40.2 + §41.5. v0 does NOT pretend to have a
working semantic cache — it reports ``"not_initialized"`` until the
chromadb container is up (v1 / `--profile obs`).
"""
